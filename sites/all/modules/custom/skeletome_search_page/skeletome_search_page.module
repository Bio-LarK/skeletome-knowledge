<?php

/**
* Implements hook_menu().
*
*/
function skeletome_profile_menu() {

    $items['search/%'] = array(
    'page callback' 	=> 'page_skeletome_search',
    'page arguments'    => array(1),
    'access arguments'  => array('access content'),
    'type'  => MENU_CALLBACK,
    );

    return $items;
}


function page_skeletome_search($term) {

    echo "now page";
}